-- take3CSR


Derivation of

take(3)$get2(CSR)

Simplifying the preceding term (1 step) leads to 

take(3)$get2(mu client server requests)

Simplifying the preceding term (1 step) leads to 

take(3)$(get0(mu client server requests)$0)$get1(mu client server requests)$
get2(mu client server requests)

Simplifying the preceding term (1 step) leads to 

take(3)$
(fun(a,fun((b:s),a:((get0(mu client server requests)$mkRequest(b))$s)))$0)$
get1(mu client server requests)$get2(mu client server requests)

Simplifying the preceding term (1 step) leads to 

take(3)$fun((b:s),0:((get0(mu client server requests)$mkRequest(b))$s))$
get1(mu client server requests)$get2(mu client server requests)

Simplifying the preceding term (1 step) leads to 

take(3)$
(0:
 ((get0(mu client server requests)$
   mkRequest(head(get1(mu client server requests)$
                  get2(mu client server requests))))$
  tail(get1(mu client server requests)$get2(mu client server requests))))

Simplifying the preceding term (1 step) leads to 

0:
(take(3-1)$
 (get0(mu client server requests)$
  mkRequest(head(get1(mu client server requests)$
                 get2(mu client server requests))))$
 tail(get1(mu client server requests)$get2(mu client server requests)))

Simplifying the preceding term (1 step) leads to 

0:
(take(2)$
 (get0(mu client server requests)$
  mkRequest(head(get1(mu client server requests)$
                 get2(mu client server requests))))$
 tail(get1(mu client server requests)$get2(mu client server requests)))

Simplifying the preceding term (1 step) leads to 

0:
(take(2)$
 (get0(mu client server requests)$(*2)$
  head(get1(mu client server requests)$get2(mu client server requests)))$
 tail(get1(mu client server requests)$get2(mu client server requests)))

Simplifying the preceding term (1 step) leads to 

0:
(take(2)$
 (get0(mu client server requests)$
  (head(get1(mu client server requests)$get2(mu client server requests))*2))$
 tail(get1(mu client server requests)$get2(mu client server requests)))

Simplifying the preceding term (1 step) leads to 

0:
(take(2)$
 (fun(a,fun((b:s),a:((get0(mu client server requests)$mkRequest(b))$s)))$
  (head(get1(mu client server requests)$get2(mu client server requests))*2))$
 tail(get1(mu client server requests)$get2(mu client server requests)))

Simplifying the preceding term (1 step) leads to 

0:
(take(2)$
 fun((b:s),
     (head(get1(mu client server requests)$get2(mu client server requests))*2):
     ((get0(mu client server requests)$mkRequest(b))$s))$
 tail(get1(mu client server requests)$get2(mu client server requests)))

Simplifying the preceding term (1 step) leads to 

0:
(take(2)$
 ((head(get1(mu client server requests)$get2(mu client server requests))*2):
  ((get0(mu client server requests)$
    mkRequest(head(tail(get1(mu client server requests)$
                        get2(mu client server requests)))))$
   tail(tail(get1(mu client server requests)$get2(mu client server requests))))))

Simplifying the preceding term (1 step) leads to 

0:
((head(get1(mu client server requests)$get2(mu client server requests))*2):
 (take(2-1)$
  (get0(mu client server requests)$
   mkRequest(head(tail(get1(mu client server requests)$
                       get2(mu client server requests)))))$
  tail(tail(get1(mu client server requests)$get2(mu client server requests)))))

Simplifying the preceding term (1 step) leads to 

0:
((head(get1(mu client server requests)$get2(mu client server requests))*2):
 (take(1)$
  (get0(mu client server requests)$
   mkRequest(head(tail(get1(mu client server requests)$
                       get2(mu client server requests)))))$
  tail(tail(get1(mu client server requests)$get2(mu client server requests)))))

Simplifying the preceding term (1 step) leads to 

0:
((head(get1(mu client server requests)$get2(mu client server requests))*2):
 (take(1)$
  (get0(mu client server requests)$(*2)$
   head(tail(get1(mu client server requests)$get2(mu client server requests))))$
  tail(tail(get1(mu client server requests)$get2(mu client server requests)))))

Simplifying the preceding term (1 step) leads to 

0:
((head(get1(mu client server requests)$get2(mu client server requests))*2):
 (take(1)$
  (get0(mu client server requests)$
   (head(tail(get1(mu client server requests)$get2(mu client server requests)))*
    2))$
  tail(tail(get1(mu client server requests)$get2(mu client server requests)))))

Simplifying the preceding term (1 step) leads to 

0:
((head(fun((a:s),mkResponse(a):(get1(mu client server requests)$s))$
       get2(mu client server requests))*
  2):
 (take(1)$
  (get0(mu client server requests)$
   (head(tail(get1(mu client server requests)$get2(mu client server requests)))*
    2))$
  tail(tail(get1(mu client server requests)$get2(mu client server requests)))))

Simplifying the preceding term (1 step) leads to 

0:
((head(mkResponse(head(get2(mu client server requests))):
       (get1(mu client server requests)$tail(get2(mu client server requests))))*
  2):
 (take(1)$
  (get0(mu client server requests)$
   (head(tail(get1(mu client server requests)$get2(mu client server requests)))*
    2))$
  tail(tail(get1(mu client server requests)$get2(mu client server requests)))))

Simplifying the preceding term (1 step) leads to 

0:
((mkResponse(head(get2(mu client server requests)))*2):
 (take(1)$
  (get0(mu client server requests)$
   (head(tail(get1(mu client server requests)$get2(mu client server requests)))*
    2))$
  tail(tail(get1(mu client server requests)$get2(mu client server requests)))))

Simplifying the preceding term (1 step) leads to 

0:
((((+1)$head(get2(mu client server requests)))*2):
 (take(1)$
  (get0(mu client server requests)$
   (head(tail(get1(mu client server requests)$get2(mu client server requests)))*
    2))$
  tail(tail(get1(mu client server requests)$get2(mu client server requests)))))

Simplifying the preceding term (1 step) leads to 

0:
(((head(get2(mu client server requests))+1)*2):
 (take(1)$
  (get0(mu client server requests)$
   (head(tail(get1(mu client server requests)$get2(mu client server requests)))*
    2))$
  tail(tail(get1(mu client server requests)$get2(mu client server requests)))))

Simplifying the preceding term (1 step) leads to 

0:
(((head((get0(mu client server requests)$0)$get1(mu client server requests)$
        get2(mu client server requests))+
   1)*
  2):
 (take(1)$
  (get0(mu client server requests)$
   (head(tail(get1(mu client server requests)$get2(mu client server requests)))*
    2))$
  tail(tail(get1(mu client server requests)$get2(mu client server requests)))))

Simplifying the preceding term (1 step) leads to 

0:
(((head((fun(a,fun((b:s),a:((get0(mu client server requests)$mkRequest(b))$s)))$
         0)$
        get1(mu client server requests)$get2(mu client server requests))+
   1)*
  2):
 (take(1)$
  (get0(mu client server requests)$
   (head(tail(get1(mu client server requests)$get2(mu client server requests)))*
    2))$
  tail(tail(get1(mu client server requests)$get2(mu client server requests)))))

Simplifying the preceding term (1 step) leads to 

0:
(((head(fun((b:s),0:((get0(mu client server requests)$mkRequest(b))$s))$
        get1(mu client server requests)$get2(mu client server requests))+
   1)*
  2):
 (take(1)$
  (get0(mu client server requests)$
   (head(tail(get1(mu client server requests)$get2(mu client server requests)))*
    2))$
  tail(tail(get1(mu client server requests)$get2(mu client server requests)))))

Simplifying the preceding term (1 step) leads to 

0:
(((head(0:
        ((get0(mu client server requests)$
          mkRequest(head(get1(mu client server requests)$
                         get2(mu client server requests))))$
         tail(get1(mu client server requests)$get2(mu client server requests))))+
   1)*
  2):
 (take(1)$
  (get0(mu client server requests)$
   (head(tail(get1(mu client server requests)$get2(mu client server requests)))*
    2))$
  tail(tail(get1(mu client server requests)$get2(mu client server requests)))))

Simplifying the preceding term (1 step) leads to 

0:
(((0+1)*2):
 (take(1)$
  (get0(mu client server requests)$
   (head(tail(get1(mu client server requests)$get2(mu client server requests)))*
    2))$
  tail(tail(get1(mu client server requests)$get2(mu client server requests)))))

Simplifying the preceding term (1 step) leads to 

0:
(2:
 (take(1)$
  (get0(mu client server requests)$
   (head(tail(get1(mu client server requests)$get2(mu client server requests)))*
    2))$
  tail(tail(get1(mu client server requests)$get2(mu client server requests)))))

Simplifying the preceding term (1 step) leads to 

0:
(2:
 (take(1)$
  (fun(a,fun((b:s),a:((get0(mu client server requests)$mkRequest(b))$s)))$
   (head(tail(get1(mu client server requests)$get2(mu client server requests)))*
    2))$
  tail(tail(get1(mu client server requests)$get2(mu client server requests)))))

Simplifying the preceding term (1 step) leads to 

0:
(2:
 (take(1)$
  fun((b:s),
      (head(tail(get1(mu client server requests)$
                 get2(mu client server requests)))*
       2):
      ((get0(mu client server requests)$mkRequest(b))$s))$
  tail(tail(get1(mu client server requests)$get2(mu client server requests)))))

Simplifying the preceding term (1 step) leads to 

0:
(2:
 (take(1)$
  ((head(tail(get1(mu client server requests)$get2(mu client server requests)))*
    2):
   ((get0(mu client server requests)$
     mkRequest(head(tail(tail(get1(mu client server requests)$
                              get2(mu client server requests))))))$
    tail(tail(tail(get1(mu client server requests)$
                   get2(mu client server requests))))))))

Simplifying the preceding term (1 step) leads to 

0:
(2:
 ((head(tail(get1(mu client server requests)$get2(mu client server requests)))*
   2):
  (take(1-1)$
   (get0(mu client server requests)$
    mkRequest(head(tail(tail(get1(mu client server requests)$
                             get2(mu client server requests))))))$
   tail(tail(tail(get1(mu client server requests)$
                  get2(mu client server requests)))))))

Simplifying the preceding term (1 step) leads to 

0:
(2:
 ((head(tail(get1(mu client server requests)$get2(mu client server requests)))*
   2):
  (take(0)$
   (get0(mu client server requests)$
    mkRequest(head(tail(tail(get1(mu client server requests)$
                             get2(mu client server requests))))))$
   tail(tail(tail(get1(mu client server requests)$
                  get2(mu client server requests)))))))

Simplifying the preceding term (1 step) leads to 

0:
(2:
 ((head(tail(get1(mu client server requests)$get2(mu client server requests)))*
   2):
  []))

Simplifying the preceding term (1 step) leads to 

0:
(2:
 [head(tail(get1(mu client server requests)$get2(mu client server requests)))*2])

Simplifying the preceding term (1 step) leads to 

0:
[2,
 head(tail(get1(mu client server requests)$get2(mu client server requests)))*2]

Simplifying the preceding term (1 step) leads to 

[0,2,
 head(tail(get1(mu client server requests)$get2(mu client server requests)))*2]

Simplifying the preceding term (1 step) leads to 

[0,2,
 head(tail(fun((a:s),mkResponse(a):(get1(mu client server requests)$s))$
           get2(mu client server requests)))*
 2]

Simplifying the preceding term (1 step) leads to 

[0,2,
 head(tail(mkResponse(head(get2(mu client server requests))):
           (get1(mu client server requests)$
            tail(get2(mu client server requests)))))*
 2]

Simplifying the preceding term (1 step) leads to 

[0,2,
 head(get1(mu client server requests)$tail(get2(mu client server requests)))*2]

Simplifying the preceding term (1 step) leads to 

[0,2,
 head(fun((a:s),mkResponse(a):(get1(mu client server requests)$s))$
      tail(get2(mu client server requests)))*
 2]

Simplifying the preceding term (1 step) leads to 

[0,2,
 head(mkResponse(head(tail(get2(mu client server requests)))):
      (get1(mu client server requests)$
       tail(tail(get2(mu client server requests)))))*
 2]

Simplifying the preceding term (1 step) leads to 

[0,2,mkResponse(head(tail(get2(mu client server requests))))*2]

Simplifying the preceding term (1 step) leads to 

[0,2,((+1)$head(tail(get2(mu client server requests))))*2]

Simplifying the preceding term (1 step) leads to 

[0,2,(head(tail(get2(mu client server requests)))+1)*2]

Simplifying the preceding term (1 step) leads to 

[0,2,
 (head(tail((get0(mu client server requests)$0)$get1(mu client server requests)$
            get2(mu client server requests)))+
  1)*
 2]

Simplifying the preceding term (1 step) leads to 

[0,2,
 (head(tail((fun(a,
                 fun((b:s),a:((get0(mu client server requests)$mkRequest(b))$s)))$
             0)$
            get1(mu client server requests)$get2(mu client server requests)))+
  1)*
 2]

Simplifying the preceding term (1 step) leads to 

[0,2,
 (head(tail(fun((b:s),0:((get0(mu client server requests)$mkRequest(b))$s))$
            get1(mu client server requests)$get2(mu client server requests)))+
  1)*
 2]

Simplifying the preceding term (1 step) leads to 

[0,2,
 (head(tail(0:
            ((get0(mu client server requests)$
              mkRequest(head(get1(mu client server requests)$
                             get2(mu client server requests))))$
             tail(get1(mu client server requests)$
                  get2(mu client server requests)))))+
  1)*
 2]

Simplifying the preceding term (1 step) leads to 

[0,2,
 (head((get0(mu client server requests)$
        mkRequest(head(get1(mu client server requests)$
                       get2(mu client server requests))))$
       tail(get1(mu client server requests)$get2(mu client server requests)))+
  1)*
 2]

Simplifying the preceding term (1 step) leads to 

[0,2,
 (head((get0(mu client server requests)$(*2)$
        head(get1(mu client server requests)$get2(mu client server requests)))$
       tail(get1(mu client server requests)$get2(mu client server requests)))+
  1)*
 2]

Simplifying the preceding term (1 step) leads to 

[0,2,
 (head((get0(mu client server requests)$
        (head(get1(mu client server requests)$get2(mu client server requests))*
         2))$
       tail(get1(mu client server requests)$get2(mu client server requests)))+
  1)*
 2]

Simplifying the preceding term (1 step) leads to 

[0,2,
 (head((fun(a,fun((b:s),a:((get0(mu client server requests)$mkRequest(b))$s)))$
        (head(get1(mu client server requests)$get2(mu client server requests))*
         2))$
       tail(get1(mu client server requests)$get2(mu client server requests)))+
  1)*
 2]

Simplifying the preceding term (1 step) leads to 

[0,2,
 (head(fun((b:s),
           (head(get1(mu client server requests)$
                 get2(mu client server requests))*
            2):
           ((get0(mu client server requests)$mkRequest(b))$s))$
       tail(get1(mu client server requests)$get2(mu client server requests)))+
  1)*
 2]

Simplifying the preceding term (1 step) leads to 

[0,2,
 (head((head(get1(mu client server requests)$get2(mu client server requests))*2):
       ((get0(mu client server requests)$
         mkRequest(head(tail(get1(mu client server requests)$
                             get2(mu client server requests)))))$
        tail(tail(get1(mu client server requests)$
                  get2(mu client server requests)))))+
  1)*
 2]

Simplifying the preceding term (1 step) leads to 

[0,2,
 ((head(get1(mu client server requests)$get2(mu client server requests))*2)+1)*
 2]

Simplifying the preceding term (1 step) leads to 

[0,2,
 ((head(fun((a:s),mkResponse(a):(get1(mu client server requests)$s))$
        get2(mu client server requests))*
   2)+
  1)*
 2]

Simplifying the preceding term (1 step) leads to 

[0,2,
 ((head(mkResponse(head(get2(mu client server requests))):
        (get1(mu client server requests)$tail(get2(mu client server requests))))*
   2)+
  1)*
 2]

Simplifying the preceding term (1 step) leads to 

[0,2,((mkResponse(head(get2(mu client server requests)))*2)+1)*2]

Simplifying the preceding term (1 step) leads to 

[0,2,((((+1)$head(get2(mu client server requests)))*2)+1)*2]

Simplifying the preceding term (1 step) leads to 

[0,2,(((head(get2(mu client server requests))+1)*2)+1)*2]

Simplifying the preceding term (1 step) leads to 

[0,2,
 (((head((get0(mu client server requests)$0)$get1(mu client server requests)$
         get2(mu client server requests))+
    1)*
   2)+
  1)*
 2]

Simplifying the preceding term (1 step) leads to 

[0,2,
 (((head((fun(a,fun((b:s),a:((get0(mu client server requests)$mkRequest(b))$s)))$
          0)$
         get1(mu client server requests)$get2(mu client server requests))+
    1)*
   2)+
  1)*
 2]

Simplifying the preceding term (1 step) leads to 

[0,2,
 (((head(fun((b:s),0:((get0(mu client server requests)$mkRequest(b))$s))$
         get1(mu client server requests)$get2(mu client server requests))+
    1)*
   2)+
  1)*
 2]

Simplifying the preceding term (1 step) leads to 

[0,2,
 (((head(0:
         ((get0(mu client server requests)$
           mkRequest(head(get1(mu client server requests)$
                          get2(mu client server requests))))$
          tail(get1(mu client server requests)$get2(mu client server requests))))+
    1)*
   2)+
  1)*
 2]

Simplifying the preceding term (1 step) leads to 

[0,2,(((0+1)*2)+1)*2]

Simplifying the preceding term (1 step) leads to 

[0,2,6]

Number of proof steps: 66